
const routes = [
  {
    path: '/',
    component: () => import('layouts/MyLayout.vue'),
    children: [
      { path: '', component: () => import('pages/Index.vue') },
      { path: 'transport/taxi', component: () => import('pages/transport/taxi/TaxiIndex.vue') },
      { path: 'transport/taxi/:slug', component: () => import('pages/transport/taxi/TaxiService.vue') }
    ]
  }
]

// Always leave this as last one
if (process.env.MODE !== 'ssr') {
  routes.push({
    path: '*',
    component: () => import('pages/Error404.vue')
  })
}

export default routes
